experiment:
  name: template_experiment

training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.001

dataset:
  name: cifar10
  validation_split: 0.2

model:
  architecture: cnn
  dropout_rate: 0.3

tracking:
  use_mlflow: true